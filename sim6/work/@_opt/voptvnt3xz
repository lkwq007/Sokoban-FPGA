library verilog;
use verilog.vl_types.all;
entity arrow_rom is
    port(
        addr            : in     vl_logic_vector(4 downto 0);
        dout            : out    vl_logic_vector(7 downto 0)
    );
end arrow_rom;
